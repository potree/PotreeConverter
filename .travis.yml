language: cpp
dist: trusty
sudo: false
script: make
compiler:
  - clang
  - gcc
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - llvm-toolchain-trusty-4.0
    packages:
    - gcc-6
    - g++-6
    - cmake
    - boost
    - clang-4.0
